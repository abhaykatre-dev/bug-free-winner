[build]
builder = "nixpacks"
buildCommand = "python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r backend/requirements.txt"

[deploy]
startCommand = ". /opt/venv/bin/activate && cd backend && gunicorn app:app -b 0.0.0.0:$PORT"
